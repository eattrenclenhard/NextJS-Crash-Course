const Topic = () => {
  return <div>Topic</div>;
};
export default Topic;

// in nextjs, when I do not specify style for my component, global.css will activate?
// Yes, if you don't specify styles for your component in Next.js, the global styles defined in `global.css` will apply, as long as they are not overridden by more specific styles in the component or other CSS files.
